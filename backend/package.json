{
  "name": "ellipse-backend",
  "version": "0.1.0",
  "description": "Ellipse Desk — Firebase Cloud Functions (agent layer)",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "googleapis": "^173.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "private": true
}
